# This directory holds secrets and is gitignored.
# Expected files:
#   pull-secret.json   — Red Hat pull secret
#   id_rsa.pub         — SSH public key for cluster nodes
#
# Run scripts/setup.sh to populate these interactively.
