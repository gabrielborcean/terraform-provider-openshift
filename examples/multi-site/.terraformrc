# Terraform CLI config — connected deployments
#
# Caches provider downloads so `terraform init` doesn't re-download on every run.
# Safe for all environments; does not prevent registry access.
plugin_cache_dir = "$HOME/.terraform.d/plugin-cache"
